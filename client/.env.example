# Optional: absolute base URL for the catalog API.
# Leave unset in local dev — the Vite proxy forwards "/api" to the Express
# server on port 3001, and the app falls back to bundled JSON if it's offline.
# Example for a deployed API:
# VITE_API_URL=https://your-api.example.com
